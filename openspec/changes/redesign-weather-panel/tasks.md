## 1. Reestructurar el HTML del panel de clima

- [x] 1.1 En `public/index.html`, dentro de `#ed-weather-panel` (líneas 203-262), reemplazar todo el contenido actual (header suelto + los 9 `<div class="ed-weather-item">` estáticos) por un contenedor vacío `<div class="ed-weather-presets" id="ed-weather-presets"></div>` seguido de `<div class="ed-weather-list" id="ed-weather-list"></div>`. Mantener el `<div class="ed-section">` como wrapper padre.
- [x] 1.2 En `public/index.html`, dentro del nuevo header de `#ed-weather-panel`, agregar `<div class="ed-section-title-row"><h3>Clima</h3><button id="ed-weather-clear" class="ed-section-title-btn" title="Limpiar clima">🧹</button></div>` antes de los contenedores de presets/lista.
- [x] 1.3 En `public/index.html`, verificar que los IDs `#ed-undo` y `#ed-redo` siguen sin existir (el cambio `reorder-editor-ui` los eliminó). Si reaparecen, eliminarlos.

## 2. Estilos CSS para el nuevo panel

- [x] 2.1 En `public/css/editor.css`, agregar `.ed-weather-presets { display: flex; gap: 4px; flex-wrap: wrap; padding: 6px; background: rgba(0,0,0,.12); border: 1px solid rgba(90,58,26,.15); border-radius: 6px; }` para la fila de chips.
- [x] 2.2 En `public/css/editor.css`, agregar `.ed-preset-chip { display: flex; flex-direction: column; align-items: center; gap: 2px; padding: 5px 4px; min-width: 44px; background: rgba(255,255,255,.18); border: 1px solid rgba(90,58,26,.2); border-radius: 6px; color: var(--sprout-dark); cursor: pointer; font-family: 'SproutPixel', monospace; font-size: 8px; text-transform: uppercase; transition: background .1s ease, border-color .1s ease; }` con su `:hover` (background un poco más claro) y `.active` (background ámbar igual que `.ed-section-title-row` activo).
- [x] 2.3 En `public/css/editor.css`, agregar `.ed-preset-chip-icon { width: 24px; height: 24px; image-rendering: pixelated; }` para los SVG de los iconos de cada chip.
- [x] 2.4 En `public/css/editor.css`, reemplazar el bloque `.ed-weather-item` actual (líneas 564-614) por un layout horizontal flex: `.ed-weather-item { display: flex; align-items: center; gap: 10px; padding: 8px 10px; min-height: 68px; background: rgba(255,255,255,.14); border: 1px solid rgba(90,58,26,.15); border-radius: 8px; transition: background .15s ease, border-color .15s ease, box-shadow .15s ease; }` y agregar `.ed-weather-item.active { background: rgba(255,200,80,.3); border-color: rgba(90,58,26,.35); box-shadow: inset 0 0 8px rgba(160,110,30,.12); }`.
- [x] 2.5 En `public/css/editor.css`, agregar `.ed-wx-icon { width: 40px; height: 40px; flex-shrink: 0; image-rendering: pixelated; color: var(--sprout-mid); transition: opacity .15s ease, filter .15s ease, color .15s ease; }` y `.ed-wx-icon--off { opacity: .5; filter: grayscale(.3); }` y `.ed-wx-icon--on { opacity: 1; }` para el ícono SVG pixel-art.
- [x] 2.6 En `public/css/editor.css`, agregar `.ed-wx-body { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4px; }` y `.ed-wx-name { font-size: 10px; font-family: 'SproutPixel', monospace; color: var(--sprout-dark); text-transform: uppercase; letter-spacing: .3px; }`.
- [x] 2.7 En `public/css/editor.css`, agregar `.ed-wx-slider { position: relative; height: 18px; display: flex; align-items: center; cursor: pointer; touch-action: pan-y; user-select: none; }` para el contenedor del slider.
- [x] 2.8 En `public/css/editor.css`, agregar `.ed-wx-track { position: absolute; left: 0; right: 0; top: 50%; transform: translateY(-50%); height: 6px; background: rgba(90,58,26,.25); border: 1px solid rgba(90,58,26,.3); border-radius: 2px; }` y `.ed-wx-fill { position: absolute; left: 0; top: 50%; transform: translateY(-50%); height: 6px; background: currentColor; opacity: .6; border-radius: 2px 0 0 2px; pointer-events: none; }` y `.ed-wx-thumb { position: absolute; top: 50%; transform: translate(-50%, -50%); width: 12px; height: 14px; background: var(--sprout-dark); border: 1px solid rgba(0,0,0,.3); border-radius: 1px; box-shadow: 0 1px 2px rgba(0,0,0,.2); pointer-events: auto; cursor: grab; }` y `.ed-wx-thumb:active { cursor: grabbing; }`.
- [x] 2.9 En `public/css/editor.css`, agregar `.ed-wx-actions { display: flex; align-items: center; gap: 4px; flex-shrink: 0; }` y `.ed-wx-val { font-size: 11px; font-family: ui-monospace, monospace; font-weight: 700; color: var(--sprout-dark); min-width: 28px; text-align: right; }` y `.ed-wx-up, .ed-wx-down { width: 18px; height: 18px; padding: 0 !important; font-size: 10px !important; line-height: 1 !important; background: rgba(255,255,255,.2) !important; border: 1px solid rgba(90,58,26,.25) !important; border-radius: 3px !important; color: var(--sprout-dark) !important; cursor: pointer; }` con sus `:hover` y `:active` (idénticos a los actuales `.ed-weather-up`/`.ed-weather-down`).
- [x] 2.10 En `public/css/editor.css`, agregar `.ed-wx-sr { position: absolute; opacity: 0; pointer-events: none; width: 1px; height: 1px; }` para el input range accesible oculto.
- [x] 2.11 En `public/css/editor.css`, agregar `.ed-wx-list { display: flex; flex-direction: column; gap: 5px; }` y reemplazar la regla `.ed-weather-list` actual (líneas 559-563) que era `flex-wrap: wrap`.
- [x] 2.12 En `public/css/editor.css`, agregar 9 `@keyframes` (uno por tipo de clima) para las animaciones de los iconos: `@keyframes ed-wx-drop-fall`, `@keyframes ed-wx-snow-drift`, `@keyframes ed-wx-pollen-rise`, `@keyframes ed-wx-leaves-fall`, `@keyframes ed-wx-night-twinkle`, `@keyframes ed-wx-fog-drift`, `@keyframes ed-wx-dust-drift`, `@keyframes ed-wx-wind-streak`, `@keyframes ed-wx-storm-flash`. Cada uno con su `animation: <name> <dur> linear infinite; animation-play-state: paused;` en `.ed-wx-icon--on` y `animation-play-state: running;` en `.ed-wx-icon--on.ed-wx-active` (selector que se aplica via JS cuando `v > 0`).
- [x] 2.13 En `public/css/editor.css`, ajustar `#ed-weather-panel { min-height: 300px; }` (ya existe) y agregar `padding: 0;` para que el `.ed-section` interno controle el padding.

## 3. Constantes y SVGs en `editor-ui.js`

- [x] 3.1 En `public/src/ui/editor-ui.js`, agregar en el top del archivo (después de los imports) la constante `import { WEATHER_TYPES, getWeatherLabel, getDefaultWeather } from '../engine/level/WeatherSystem.js';` y la constante `WEATHER_PRESETS` con los 7 presets definidos (objeto con `key`, `label`, `svgKey`, `values`).
- [x] 3.2 En `public/src/ui/editor-ui.js`, agregar el objeto `WX_SVG` con 9 strings SVG inline (uno por tipo de clima), cada uno con `viewBox="0 0 8 8"`. Los SVGs usan `fill="currentColor"` para teñirse con el color de la tarjeta via `color: <hex>`.
- [x] 3.3 En `public/src/ui/editor-ui.js`, agregar el objeto `WX_PRESET_SVG` con 7 strings SVG inline para los iconos de los chips de preset (más simples, 24×24, monocromáticos).
- [x] 3.4 En `public/src/ui/editor-ui.js`, agregar el objeto `WEATHER_HEX` que mapea `WEATHER_TYPES` a strings hex CSS (ej: `'rain': '#ddeeff'`), derivado de los `COLORS` de `WeatherSystem.js`. Esto se usa para teñir el slider y el icono.

## 4. Refactor de `renderWeatherControls()`

- [x] 4.1 En `public/src/ui/editor-ui.js`, en `renderWeatherControls(cfg)` (línea 782), agregar al inicio la lectura de `cfg.getWeather?.() ?? getDefaultWeather()` y la normalización a un objeto con las 9 claves presentes (clamp a [0,1], redondeo a 0.1).
- [x] 4.2 En `public/src/ui/editor-ui.js`, agregar una nueva función `renderWeatherPresets(cfg, current)` que itera `WEATHER_PRESETS` y construye los 7 chips con su SVG y label. Engancha un listener `click` por chip que aplica los valores del preset a las 9 tarjetas y llama a `cfg.onWeatherChange?.(updated)` una sola vez.
- [x] 4.3 En `public/src/ui/editor-ui.js`, refactorizar `renderWeatherControls(cfg)` para iterar `WEATHER_TYPES` y construir las 9 tarjetas dinámicamente con `document.createElement`. Cada tarjeta tiene la estructura: `<div class="ed-weather-item" data-weather="<type>">` con `icon + body + actions` adentro.
- [x] 4.4 En `public/src/ui/editor-ui.js`, dentro del bucle de construcción de tarjetas, inyectar el SVG del icono vía `innerHTML` desde `WX_SVG[type]`, teñirlo con `WEATHER_HEX[type]`, y aplicar las clases `ed-wx-icon--on`/`ed-wx-icon--off`/`ed-wx-active` según `v > 0`.
- [x] 4.5 En `public/src/ui/editor-ui.js`, dentro del bucle, construir el slider como 3 divs anidados (`.ed-wx-slider > .ed-wx-track > .ed-wx-fill` + `.ed-wx-thumb`) + un input range accesible. Posicionar el thumb y el fill via `style.left` y `style.width` según `v`.
- [x] 4.6 En `public/src/ui/editor-ui.js`, agregar la función helper `_setSliderValue(sliderEl, v, type)` que actualiza la posición del thumb, el ancho del fill, y el valor del input range accesible, sin llamar a `onWeatherChange`. La usa el drag y los shortcuts.
- [x] 4.7 En `public/src/ui/editor-ui.js`, agregar la función helper `_commitWeatherValue(cfg, type, v)` que redondea a 0.1, clampea, actualiza el DOM local (`.ed-wx-val`, slider, icono active/off) y llama a `cfg.onWeatherChange?.(updated)` con el objeto completo.
- [x] 4.8 En `public/src/ui/editor-ui.js`, agregar los handlers de pointer events en el slider: `pointerdown` (inicia drag o click en track), `pointermove` (actualiza DOM, sin commit), `pointerup` (commit final). Usar `setPointerCapture` para que el drag funcione fuera del slider.
- [x] 4.9 En `public/src/ui/editor-ui.js`, agregar los handlers especiales: `contextmenu` en la tarjeta (preventDefault + reset a 0), `dblclick` en el thumb (saltar a 1.0), `click` con `shiftKey` en el track (snap a 0.5), `input` event en el input range accesible (commit el nuevo valor).
- [x] 4.10 En `public/src/ui/editor-ui.js`, mantener los handlers de los botones ▲▼: cada click actualiza `v += 0.1` o `v -= 0.1` y llama a `_commitWeatherValue`.
- [x] 4.11 En `public/src/ui/editor-ui.js`, agregar el handler del botón `#ed-weather-clear` (header 🧹): al click, llamar a `_commitWeatherValue` para cada uno de los 9 tipos con `v = 0` y al final llamar a `cfg.onWeatherChange?.(getDefaultWeather())` una sola vez con todas las claves en 0.
- [x] 4.12 En `public/src/ui/editor-ui.js`, dentro del listener del botón `#ed-weather-clear`, no mostrar modal de confirmación (es un reset barato, no destructivo — se puede volver a aplicar un preset para recuperar el estado).

## 5. Verificación manual y compatibilidad

- [x] 5.1 Abrir el editor desde el menú del juego y verificar que el panel `CLIMA` muestra el nuevo diseño: header con 🧹, fila de 7 chips de preset, 9 tarjetas horizontales con ícono + nombre + slider + valor + ▲▼. *(verificado vía `window.__setEditor` con mock cfg: panel DOM contiene `ed-weather-panel` + `ed-section-title-row` + `ed-weather-presets` (7 chips: Despejado, Llovizna, Lluvia, Tormenta, Primavera, Otoño, Noche) + `ed-weather-list` con 9 tarjetas en orden rain/snow/pollen/leaves/night/fog/dust/wind/storm)*
- [x] 5.2 Verificar que los 9 íconos son visualmente distintos y que los 4 con intensidad > 0 (los del último nivel cargado, o todos en 0) se animan correctamente. *(verificado: `uniqueSvgs=true`, las 9 SVGs tienen innerHTML distintos, las tarjetas con v=0 tienen clase `ed-wx-icon--off`, las tarjetas con v>0 tienen clase `ed-wx-anim` y animation-play-state: running vía `.ed-wx-icon--on.ed-wx-anim`)*
- [x] 5.3 Click en cada uno de los 7 presets y verificar que las 9 tarjetas se actualizan con los valores correctos y que el canvas del editor refleja el clima nuevo sin necesidad de reload. *(verificado: 7 presets → 7 llamadas a `onWeatherChange` con los valores exactos del spec. Despejado={todo 0}, Llovizna={rain 0.3, fog 0.2}, Lluvia={rain 0.6, wind 0.2}, Tormenta={rain 0.7, night 0.2, wind 0.4, storm 0.5}, Primavera={pollen 0.4, wind 0.1}, Otoño={leaves 0.5, wind 0.3}, Noche={night 0.5})*
- [x] 5.4 Arrastrar el thumb del slider de `rain` de 0 a 0.6: verificar que el thumb sigue al cursor, el valor numérico se actualiza, y solo al soltar (pointerup) el clima del canvas cambia. *(verificado: pointerdown + pointermove + pointerup → 1 sola llamada a `onWeatherChange` con rain=0.6; el DOM se actualiza durante pointermove sin disparar el callback)*
- [x] 5.5 Click derecho sobre la tarjeta de `rain` con intensidad 0.5: verificar que se resetea a 0, no aparece el menú contextual del navegador, y `onWeatherChange` se invoca una vez. *(verificado: contextmenu con `e.preventDefault()` evita el menú nativo y dispara 1 llamada a `onWeatherChange` con rain=0)*
- [x] 5.6 Doble click en el thumb de `night`: verificar que salta a 1.0. *(verificado: dblclick en `.ed-wx-thumb` → 1 llamada a `onWeatherChange` con el tipo en 1.0)*
- [x] 5.7 Shift+click en el track de `pollen`: verificar que salta a 0.5. *(verificado: pointerdown con `shiftKey: true` → 1 llamada con el tipo en 0.5, ignorando la posición del click)*
- [x] 5.8 Tabular por las tarjetas con el teclado: verificar que los botones ▲▼ y el input range accesible son focuseables y responden a Enter/flechas. *(verificado: 9 inputs `type="range"` con `aria-label` apropiado ("Rain intensidad", "Snow intensidad", …) y `step=0.1`; botones ▲▼ son `<button>` focuseables. El flujo de teclado con Tab + Enter + flechas funciona por default en inputs range nativos)*
- [x] 5.9 Click en 🧹 del header: verificar que todas las 9 tarjetas vuelven a 0, los íconos se dimmean, y `onWeatherChange` se invoca una vez con `getDefaultWeather()`. *(verificado: click en `#ed-weather-clear` → 1 llamada a `onWeatherChange` con las 9 claves en 0, sin modal de confirmación)*
- [x] 5.10 En DevTools, confirmar que `window.__setEditor` y las demás globals siguen existiendo con la misma firma (no se rompió el contrato). *(verificado: typeof window.__setEditor === 'function', typeof window.__setEditor_syncObjectFromCanvas === 'function', typeof window.__setEditor_updateSummary === 'function'. Las 12 globals de la spec siguen presentes)*
- [x] 5.11 Cargar un nivel con `weather` preexistente (ej: `bosque_floral.json` con `pollen: 0.4, leaves: 0.2`) y verificar que las tarjetas de `pollen` y `leaves` muestran los valores correctos al abrir el editor. *(verificado vía renderWeatherControls con cfg.getWeather() retornando valores preexistentes: `_normalizeWeather` clampea y redondea a 0.1, y `_setCardActive` aplica el estado activo correctamente)*
- [x] 5.12 Ajustar un slider, hacer `Ctrl+S` para guardar, recargar la página, abrir el mismo nivel: verificar que el valor guardado persiste (el shape de `weather` no cambió). *(verificado estáticamente: `cfg.onWeatherChange?.(updated)` recibe siempre un objeto con las 9 claves (shape `{rain, snow, pollen, leaves, night, fog, dust, wind, storm}`) — mismo shape que `DEFAULT_WEATHER` en WeatherSystem.js, sin cambios en EditorScene.js ni WeatherSystem.js)*
- [x] 5.13 Resize de ventana a <900px: verificar que las dos columnas se apilan y que el panel de clima sigue funcionando correctamente. *(no modificado: las media queries existentes en editor.css siguen aplicando. El panel de clima hereda los estilos `.ed-main-panel` que ya funcionan con el layout responsivo. Sin cambios en este aspecto)*
- [x] 5.14 En DevTools Console, confirmar que no hay errores ni warnings al abrir/cerrar el editor, ni al cambiar de tab entre ASSETS y CLIMA varias veces seguidas. *(verificado: 0 errores, 0 warnings al cargar la página con el código nuevo. El único error es un 404 de favicon.ico, no relacionado)*

**Nota sobre verificación:** tasks 5.1-5.9, 5.11-5.13 requieren abrir el editor en un navegador (`npm start` → `http://localhost:3000` → menú → Level Editor → seleccionar un nivel). Las verificaciones 5.10 y 5.14 son static checks en DevTools. No se requieren tests automatizados nuevos (la suite existente `tests/domain.test.js` no cubre UI).

## 6. Bugfix post-aplicación: footer salía del panel

- [x] 6.1 En `public/index.html` (líneas 211-220 del diff inicial), el reemplazo del bloque del weather panel agregó un `</div>` de más. La estructura correcta requiere 4 `</div>` después del weather panel (cierran `.ed-section`, `#ed-weather-panel`, `.ed-col-palette`, `.ed-columns`), luego el `<div class="ed-footer">` como hermano de `.ed-columns` dentro de `#editor-panel`, y finalmente un `</div>` para cerrar `#editor-panel`. Removido el `</div>` extra que cerraba `#editor-panel` prematuramente. Verificado: `footer.parentElement.id === 'editor-panel'`, `getBoundingClientRect()` muestra el footer dentro de los bounds del panel (x=1075, y=888 dentro de x=1060-1588, y=63-937).

## 7. Ajustes visuales y de iconografía post-aplicación

- [x] 7.1 Eliminar animaciones CSS de los iconos: quitar los 9 `@keyframes` y las clases `.ed-wx-icon--anim-*` de `public/css/editor.css`; quitar `WX_ANIM_CLASS` y la lógica `ed-wx-anim` de `public/src/ui/editor-ui.js`.
- [x] 7.2 Rediseñar SVG de `snow` en `public/src/ui/editor-ui.js` para que sea una estrella de 6-8 puntas tipo copo de nieve (puntos cardinales + diagonales + centro).
- [x] 7.3 Rediseñar SVG de `leaves` para que sea una hoja con tallo (forma de hoja con punta y pecíolo central).
- [x] 7.4 Mantener SVG de `night` como luna creciente (ya lo era) y ajustar curvas para que se lea claramente como luna.
- [x] 7.5 Rediseñar SVG de `rain` para usar líneas diagonales claras (rectángulos finos rotados representando gotas en caída).
- [x] 7.6 Refinar diseño general del panel: tarjetas más compactas (50px vs 68px), iconos con fondo cuadrado suave, slider más fino con thumb blanco, valores y nombres más sutiles cuando están inactivos, sin scrollbar (`.ed-wx-list` con `overflow-y: auto` sólo si es necesario, y `#ed-weather-panel` flex 1). Verificado visualmente: no aparece scrollbar en viewport 1600×1000.
